# 贡献指南

感谢你参与 `ESPboy_esp8266` 开源项目。

## 1. 贡献类型

- 硬件复刻文档完善。
- 游戏移植修复与兼容性改进。
- 新游戏提交。
- Starter 模板与工具链优化。
- Bug 修复与文档整理。

## 2. 开始前请先确认

1. 先开 Issue 说明目标与范围。
2. 确认第三方代码/素材的许可证兼容。
3. 保持改动聚焦，避免一次 PR 过大。

## 3. 仓库约定

- 游戏代码在 `games/<game_name>/`。
- 文档在 `doc/`。
- 开发模板在 `starter/`。
- 工具与脚本在 `tools/`。

每个游戏建议包含：

- `README.md`
- `manifest.json`
- 源码文件（`.ino` / `.cpp` / `.h` / 资源）

## 4. Pull Request 检查清单

- [ ] 已在本地完成编译验证（至少通过编译）。
- [ ] 若行为或流程变更，已同步更新 README。
- [ ] 涉及游戏改动时已新增或更新 `manifest.json`。
- [ ] 未提交 `node_modules` 或 IDE 工作区文件。
- [ ] 第三方来源与许可证已同步到 `THIRD_PARTY.md`。

## 5. 提交信息约定

建议使用以下前缀：

- `feat:` 新功能/新内容
- `fix:` 问题修复
- `docs:` 文档变更
- `chore:` 维护性或结构性调整
- `refactor:` 重构（无行为变化）

## 6. 测试建议

涉及游戏改动时，最低验证项：

1. 能正常启动。
2. 按键响应正确。
3. 显示渲染正常。
4. 使用 EEPROM 时，读写行为正确。
